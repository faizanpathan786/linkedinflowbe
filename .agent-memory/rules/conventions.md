# Conventions: lflow-backend

> Auto-generated by repo-memory. Add corrections in `.agent-memory/overrides/`.

## Language conventions

- **typescript**: Use strict TypeScript (`strict: true` in tsconfig).
- **typescript**: Prefer `interface` over `type` for object shapes.
- **typescript**: Avoid `any`; use `unknown` and narrow explicitly.

## Inferred project patterns

- Dependencies are managed via `package.json`. Use `npm install` or `yarn` — never edit `node_modules/` directly.
